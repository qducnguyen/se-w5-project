using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TerrainType : MonoBehaviour
{
    public enum TerrainTypes {_base, obstacle, money, background_object};
    public TerrainTypes terrainType;
}
